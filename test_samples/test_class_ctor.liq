def counter
	int _count = 25
	string _name = "empty"
	int _number
	def __init__()
		self._number = 4

counter c

display("c.count=%d (25) c.name=%s (empty) c.number=%d (4)\n", c._count, c._name, c._number)

