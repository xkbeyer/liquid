def counter_ctor
	int _count = 25
	string _name = "empty"
	int _number
	def __init__()
		self._number = 4

counter_ctor ctor

display("c.count=%d (25) c.name=%s (empty) c.number=%d (4)\n", ctor._count, ctor._name, ctor._number)

